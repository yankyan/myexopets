<div class="form-control">
  <%= form_for(molt) do |f| %>
  <% if molt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(molt.errors.count, "error") %> prohibited this molt from being saved:</h2>

      <ul>
      <% molt.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<%= params[:id]%>
    <%= f.label :date, "Дата линьки", class:"col-form-label"  %>
    <%= f.date_field :date, class:"form-control", value: Date.today %>
    <%= f.hidden_field :pet_id, :value =>  params.require(:id).to_i %>
<br>
    <%= f.submit  :Записать , class: "btn btn-success"%>
  </div>
<% end %>
